/**
 * Centralized constants for the trading dashboard.
 * When authentication is added, replace DEFAULT_USER_ID with the
 * authenticated user's ID from the session/token.
 */
export const DEFAULT_USER_ID = 'user_default';
